[project]
name = "trading-journal-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "supabase",
    "pytest",
    "fastapi",
    "uvicorn",
    "httpx",
    "pytest-cov",
]

[tool.vercel]
entrypoint = "api:app"